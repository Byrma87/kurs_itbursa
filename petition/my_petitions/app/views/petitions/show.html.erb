
<table>
   <h1>Петиция</h1>
  <td><%= @petition.title %></td>
  <tr></tr>
  <td><%= @petition.created_at.strftime("%d/%m/%y %I:%M") %></td>
  <tr></tr>
  <td><%=  @petition.text %></td>
  </table>
 
<tr></tr>
 <div class="field">
  <%= label_tag "Голосов" %>
  <%= label_tag @votes %>
</div>
<% unless  @petition.user_id.to_i == current_user.id.to_i %>
  <% unless @petition.expired_days(@petition) %>
  <th><%= button_to 'Проголосовать', votes_path({petition_id: @petition, user_id: current_user.id})%></th> 
      <% end %>
  <% end %>

